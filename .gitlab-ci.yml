cache:
  paths:
  - node_modules/

stages:
  - deploy

build:
  stage: deploy
  script:
    - npm install
    - mkdir out/syntaxes
    - mkdir out/support
    - npm run compile
  only:
    - master
    - develop
    - /^release/.*$/
    - /^testing/.*$/

deploy:
  before_script:
    - npm i -g vsce
  script:
    - vsce package
  only:
    - develop
  artifacts:
    paths:
    - out